import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primarios — verde FIFA
  static const primary = Color(0xFF006847);
  static const primaryLight = Color(0xFF1A8C61);
  static const primaryDark = Color(0xFF004D35);

  // Acento — dorado
  static const accent = Color(0xFFF5A623);
  static const accentLight = Color(0xFFF7BC56);
  static const accentDark = Color(0xFFCC8A1B);

  // Semánticos
  static const success = Color(0xFF2ECC71);
  static const warning = Color(0xFFF39C12);
  static const error = Color(0xFFE74C3C);
  static const info = Color(0xFF3498DB);

  // Neutrales
  static const white = Color(0xFFFFFFFF);
  static const background = Color(0xFFF5F6FA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF0F2F5);
  static const divider = Color(0xFFE1E4EB);

  // Texto
  static const textPrimary = Color(0xFF1A1D23);
  static const textSecondary = Color(0xFF6B7280);
  static const textDisabled = Color(0xFFB0B7C3);

  // Sticker states
  static const stickerOwned = Color(0xFF2ECC71);
  static const stickerMissing = Color(0xFFE1E4EB);
  static const stickerDuplicate = Color(0xFFF39C12);

  // Dark mode
  static const darkBackground = Color(0xFF0F1117);
  static const darkSurface = Color(0xFF1A1D26);
  static const darkSurfaceVariant = Color(0xFF252836);
}
